export enum Routes {
    HOME = "/",
    LOGIN = "/login",
    HISTORY = "/history",
    RECORD = "/record",
}

export enum ServerRoutes {
    SIGNUP = "http://127.0.0.1:5000/signup",
    LOGIN = "http://127.0.0.1:5000/login",
}